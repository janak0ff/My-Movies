/**
 * A React functional component that renders a loading indicator.
 *
 * This component is used to display a "Loading..." message while data is being fetched or processed.
 *
 * @returns {JSX.Element} A JSX element containing the loading indicator.
 */
function Loader() {
  return <p className="loader">Loading...</p>;
}

export default Loader;
